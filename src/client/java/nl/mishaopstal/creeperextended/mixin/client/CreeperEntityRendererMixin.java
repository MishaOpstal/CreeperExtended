package nl.mishaopstal.creeperextended.mixin.client;

// Placeholder class to avoid client mixin until needed. Visual spin is handled server-side by yaw updates.
public class CreeperEntityRendererMixin {
}
